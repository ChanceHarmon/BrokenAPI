# BrokenAPI
This prototype teaches students that APIs are only useful if the data matches the expected structure. Students inspect a broken JSON response, fix it using a simple schema, and immediately see their corrected data render into UI cards, which makes the debugging task feel concrete instead of abstract.
